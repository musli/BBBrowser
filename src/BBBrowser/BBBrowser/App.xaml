<Application x:Class="BBBrowser.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BBBrowser"
             xmlns:wp="clr-namespace:BBBrowser">
    <Application.Resources>
        <Style TargetType="wp:Sprite">
            <Setter Property="wp:WindowAttach.IgnoreAltF4" Value="True"/>
            <Setter Property="wp:WindowAttach.ShowInTaskManager" Value="False"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
            <!--<Setter Property="ResizeMode" Value="NoResize"/>-->
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="ShowActivated" Value="False"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="Topmost" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="wp:Sprite">
                        <ContentPresenter Margin="{TemplateBinding Padding}"  wp:WindowAttach.IsDragElement="True" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                       
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
